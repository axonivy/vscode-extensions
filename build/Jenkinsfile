pipeline {
  agent {
    dockerfile {
      filename 'build/Dockerfile'
    }
  }

  options {
    buildDiscarder(logRotator(numToKeepStr: '60', artifactNumToKeepStr: '2'))
  }

  triggers {
    cron '@midnight'
  }

  parameters {
    string(name: 'engineDownloadUrl', defaultValue: 'https://jenkins.ivyteam.io/view/my-builds/job/core_product/job/XIVY-10877_glsp_socket/13/artifact/workspace/ch.ivyteam.ivy.server.product/target/products/AxonIvyEngine11.2.0.2305231539_All_x64.zip', description: 'engine that will be packaged')
  }

  stages {
    stage('Build') {
      steps {
        script {
          sh 'yarn update:axonivy:next'
          sh 'yarn install --ignore-scripts && yarn clean'
          sh 'yarn build:production'
        }
      }
    }

    stage('Codechecks (ESLint)') {
      steps {
        script {
          catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE') {
            timeout(30) {
              sh 'yarn lint -o eslint.xml -f checkstyle'
            }
          }
        }
      }
    }

    stage('Package') {
      steps {
        script {
          sh 'yarn download:engine ' + params.engineDownloadUrl
          sh 'yarn package'
          archiveArtifacts 'vscode-extensions/**/*.vsix'
        }
      }
    }
  }
} 
